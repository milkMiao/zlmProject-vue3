import { //装饰器
    AutoIncrement,
    Column,
    Model,
    PrimaryKey,
    Table,
    AllowNull,
    Unique,
    DataType,
    CreatedAt,
    UpdatedAt
} from 'sequelize-typescript';
export class Board extends Model<Board>{
    
}